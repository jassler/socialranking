\begin{table}
\centering
\resizebox{\linewidth}{!}{
\begin{tabular}[t]{lll}
\toprule
Attribute & Description & Value in \texttt{pr}\\
\midrule
\texttt{elements} & Sorted vector of elements & \texttt{c(1,2)}\\
\midrule
\texttt{rankingCoalitions} & Coalitions in power relation & \texttt{list(set(1,2),set(2),set(),set(1))}\\
\midrule
\texttt{equivalenceClasses} & \makecell[l]{List containing lists, each\\containing coalitions in the\\same equivalence class} & \makecell[l]{\texttt{list(list(set(1,2)),}\\\texttt{list(set(2), set()),}\\\texttt{list(set(1)))}}\\
\bottomrule
\end{tabular}}
\end{table}
